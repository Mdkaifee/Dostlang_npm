# dostlang

Install globally:

```bash
npm i -g dostlang
```

Then run:

```bash
dostlang -c $'yeh_ha x = 2\ndost_bol x + 3'
```

Run a file:

```bash
dostlang hello.dost
```

Requirements:

- Node.js 18+
- Python 3 available on PATH (`python3`, `python`, or `py -3`)
